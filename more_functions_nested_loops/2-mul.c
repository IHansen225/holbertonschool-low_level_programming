#include <stdio.h>
#include "main.h"

/**
* mul - multiplies numbers
* @a: input 1
* @b: input 2
* @c: result
* Return: multiplied numbers
*/

int mul(int a, int b)
{
	int c = (a * b);

	return (c);
}
