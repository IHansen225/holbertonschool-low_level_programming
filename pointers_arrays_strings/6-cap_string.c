#include "main.h"

/**
 * cap_string: capitalizes a string
 * @a: string 
 * 
 * return: nothing
 */

char *cap_string(char *a)
{
	/**
	* char sep[13] = {',', ';', '.', '!', '?', '"', '(', ')', '{', '}', '\n', '\t', ' '};
	* int i, j;
	* int flag;
	*/
	int i, cont;

	while (s[cont] != '\0')
	{
		cont++;
	}

	for (i = 0; i == cont; i++)
	{
		a[i] = 'x';
	}

	return (a);
}
